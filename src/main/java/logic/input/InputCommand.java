package logic.input;

/**
 * Represents all possible player input commands mapped from keyboard keys.
 */
public enum InputCommand {
    NONE,
    UNDO,
    REDO,
    MOVE_UP,
    MOVE_DOWN,
    MOVE_LEFT,
    MOVE_RIGHT,
    TRIGGER,
    RESET,
    MENU
}
